fn makecounter(){
    let x = 0.0

    let gen = | |{
        x = x+1.0
        _mimium_schedule_at(now+1.0, | |{
            x = x+1.0
        })
    }
    let getter = | | {x}
    let _ = _mimium_schedule_at(1.0,gen)
    getter
}
let x_getter = makecounter();
fn dsp(){
    x_getter()
}