// counter implemented using closure. for the test of assignment expression
fn makecounter(){
    let init = 0.0
    let x = init
    let countup = | |{
        let res = x
        x = x+1.0
        res 
    }
    (countup,init)
}
let (myc,_) = makecounter();
fn dsp(){
    myc()
}