fn phasor(freq){
    (self + 6.28/freq)%1.0
}
fn sinwave(freq){
    let x = freq |>
        phasor() |>
        sin()
    let y = (freq*2.0) |>
        phasor() |>
        sin() +
        x
    y
}
fn dsp(){
    440.0 |> (|x| sinwave(x))()
}